function Navbar() {
  return (
    <h1
      className="text-blue-300 text-center mb-10 py-10 bg-gray-900 text-5xl font-semibold"
      style={{ filter: "drop-shadow(0 1px 6px #c4f3f5" }}
    >
      <span style={{ filter: "drop-shadow(0 0 6px #c4f3f5" }}>
        Spacestagram
      </span>
    </h1>
  );
}

export default Navbar;
