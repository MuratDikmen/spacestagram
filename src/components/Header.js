function Navbar() {
  return (
    <h1
      className="text-white text-center mb-10 py-10 bg-gray-900 text-5xl font-semibold"
      style={{ filter: "drop-shadow(0 1px 1px rgba(255, 255, 255, 0.5)" }}
    >
      <span style={{ filter: "drop-shadow(0 0 1px rgba(255, 255, 255, 0.5)" }}>
        Spacestagram
      </span>
    </h1>
  );
}

export default Navbar;
